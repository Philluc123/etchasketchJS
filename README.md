## Etch-A-Sketch JS </br>
A browser-based drawing application that combines the functionality of a sketchpad with the nostalgia of an Etch-A-Sketch. Built as part of The Odin Project Foundations course.
Features

Interactive grid-based drawing canvas
Responsive hover effects for drawing
Customizable grid size
Clear/reset functionality
Pure vanilla JavaScript implementation
Clean, minimalist interface

HTML5
CSS3
JavaScript (ES6+)

Installation & Usage

Clone this repository:

bash   git clone https://github.com/Philluc123/etchasketchJS.git

Navigate to the project directory:

bash   cd etchasketchJS

Open index.html in your web browser:

bash   # On macOS
   open index.html
   
   # On Linux
   xdg-open index.html
   
   # On Windows
   start index.html
Or simply drag and drop the index.html file into your browser window.
How to Use

Hover your mouse over the grid squares to draw
Use the controls to adjust grid size or clear the canvas
Create your digital artwork!

Project Structure
etchasketchJS/
│
├── index.html      # Main HTML structure
├── style.css       # Styling and layout
└── script.js       # Drawing logic and interactivity
Learning Objectives
This project demonstrates:

DOM manipulation with JavaScript
Event handling (mouse events)
Dynamic element creation
CSS Grid/Flexbox layout
Responsive design principles

Future Enhancements
Potential features to add:

Color picker for custom drawing colors
Rainbow mode with random colors
Eraser tool
Save drawing as image
Different brush sizes
Touch support for mobile devices

Built as part of The Odin Project curriculum
Inspired by the classic Etch-A-Sketch toy
